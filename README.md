# Unfiltered UK

A decentralized, privacy-first community platform where every server owns its data.

## ✨ Features
- Per-server isolated MongoDB databases
- End-to-end message encryption + platform signature verification
- IP anonymisation via HMAC + pseudonyms
- Stripe subscriptions (Free → Ultra)
- Socket.IO real-time messaging
- Full moderation, roles, audit logs

## 🚀 Setup
1. `cp .env.example .env.local` and fill secrets
2. `npm install`
3. `npm run dev`

## 📦 Stack
Next.js 14 (App Router) • MongoDB • Socket.IO • Cloudinary • Stripe • Tailwind CSS

Deployed on Render from GitHub.
